import { Check, Download, FileText, FolderKanban, LoaderCircle, MessageSquareText, Plus, X } from 'lucide-react'
import { useEffect,useState } from 'react'
import { Link,useParams } from 'react-router-dom'
import { useTenant } from '../../tenant/tenant-context'
import { portalClient,type PortalClient,type PortalDashboardSnapshot,type PortalProjectSnapshot } from './client'
const t={title:'\u0628\u0648\u0627\u0628\u0629 \u0627\u0644\u0639\u0645\u064a\u0644',projects:'\u0627\u0644\u0645\u0634\u0631\u0648\u0639\u0627\u062a',approvals:'\u0627\u0639\u062a\u0645\u0627\u062f\u0627\u062a \u0645\u0637\u0644\u0648\u0628\u0629',deliveries:'\u0627\u0644\u062a\u0633\u0644\u064a\u0645\u0627\u062a',empty:'\u0644\u0627 \u062a\u0648\u062c\u062f \u0639\u0646\u0627\u0635\u0631 \u0645\u062a\u0627\u062d\u0629.',error:'\u062a\u0639\u0630\u0631 \u062a\u062d\u0645\u064a\u0644 \u0628\u0648\u0627\u0628\u0629 \u0627\u0644\u0639\u0645\u064a\u0644.',request:'\u0637\u0644\u0628 \u062c\u062f\u064a\u062f',tasks:'\u0627\u0644\u0645\u0647\u0627\u0645 \u0627\u0644\u0645\u062a\u0627\u062d\u0629',comments:'\u0627\u0644\u062a\u0639\u0644\u064a\u0642\u0627\u062a',files:'\u0627\u0644\u0645\u0644\u0641\u0627\u062a',approve:'\u0645\u0648\u0627\u0641\u0642\u0629',changes:'\u0637\u0644\u0628 \u062a\u0639\u062f\u064a\u0644',download:'\u062a\u0646\u0632\u064a\u0644',noTenant:'\u0644\u0627 \u062a\u0648\u062c\u062f \u0639\u0636\u0648\u064a\u0629 \u0646\u0634\u0637\u0629.'}
export function PortalDashboardScreen({organizationId,slug,client}:{organizationId:string;slug:string;client:PortalClient}){const[snapshot,setSnapshot]=useState<PortalDashboardSnapshot|null>(null);const[status,setStatus]=useState<'loading'|'ready'|'error'>('loading');useEffect(()=>{let active=true;client.dashboard(organizationId).then(value=>{if(active){setSnapshot(value);setStatus('ready')}},()=>{if(active)setStatus('error')});return()=>{active=false}},[client,organizationId]);if(status==='loading')return<main dir="rtl" className="grid min-h-screen place-items-center"><p role="status"><LoaderCircle className="inline size-5 animate-spin" aria-hidden="true"/>...</p></main>;if(status==='error'||!snapshot)return<main dir="rtl"><h1>{t.error}</h1></main>;return<main dir="rtl" className="min-h-screen bg-gray-50"><header className="border-b bg-white"><div className="mx-auto max-w-5xl px-5 py-6"><h1 className="text-2xl font-black">{t.title}</h1></div></header><div className="mx-auto grid max-w-5xl gap-6 px-5 py-6 lg:grid-cols-2"><section><h2 className="flex items-center gap-2 font-black"><FolderKanban className="size-5" aria-hidden="true"/>{t.projects}</h2><div className="mt-3 space-y-3">{snapshot.projects.map(project=><Link key={project.id} to={`/portal/${slug}/projects/${project.id}`} className="block border bg-white p-4"><strong>{project.name}</strong><span className="mt-1 block text-sm text-gray-500">{project.code} \u00b7 {project.status}</span></Link>)}{!snapshot.projects.length&&<p className="border bg-white p-6 text-gray-500">{t.empty}</p>}</div></section><section><h2 className="font-black">{t.approvals}</h2><div className="mt-3 space-y-3">{snapshot.pendingApprovals.map(item=><article key={item.id} className="border bg-white p-4"><h3 className="font-bold">{item.title}</h3><p className="text-xs text-gray-500">v{item.reviewedVersion}</p><div className="mt-3 flex gap-2"><button aria-label={`${t.approve}: ${item.title}`} onClick={()=>void client.decideApproval(organizationId,{approvalId:item.id,reviewedVersion:item.reviewedVersion,decision:'approved'})} className="inline-flex items-center gap-2 rounded-md bg-teal-800 px-3 py-2 text-sm font-bold text-white"><Check className="size-4" aria-hidden="true"/>{t.approve}</button><button aria-label={`${t.changes}: ${item.title}`} onClick={()=>void client.decideApproval(organizationId,{approvalId:item.id,reviewedVersion:item.reviewedVersion,decision:'changes_requested',reason:'Client requested changes'})} className="inline-flex items-center gap-2 rounded-md border px-3 py-2 text-sm font-bold"><X className="size-4" aria-hidden="true"/>{t.changes}</button></div></article>)}{!snapshot.pendingApprovals.length&&<p className="border bg-white p-6 text-gray-500">{t.empty}</p>}</div></section><section className="lg:col-span-2"><h2 className="font-black">{t.deliveries}</h2><div className="mt-3 grid gap-3 sm:grid-cols-2">{snapshot.deliveries.map(item=><article key={item.id} className="border bg-white p-4"><FileText className="size-5" aria-hidden="true"/><h3 className="mt-2 font-bold">{item.title}</h3></article>)}{!snapshot.deliveries.length&&<p className="border bg-white p-6 text-gray-500">{t.empty}</p>}</div></section></div></main>}
export function PortalProjectScreen({organizationId,projectId,client}:{organizationId:string;projectId:string;client:PortalClient}){const[snapshot,setSnapshot]=useState<PortalProjectSnapshot|null>(null);const[status,setStatus]=useState<'loading'|'ready'|'error'>('loading');useEffect(()=>{let active=true;client.project(organizationId,projectId).then(value=>{if(active){setSnapshot(value);setStatus('ready')}},()=>{if(active)setStatus('error')});return()=>{active=false}},[client,organizationId,projectId]);if(status==='loading')return<main dir="rtl"><p role="status">...</p></main>;if(status==='error'||!snapshot)return<main dir="rtl"><h1>{t.error}</h1></main>;return<main dir="rtl" className="min-h-screen bg-gray-50 p-5"><div className="mx-auto max-w-5xl"><h1 className="text-2xl font-black">{snapshot.project.name}</h1><div className="mt-6 grid gap-6 lg:grid-cols-3"><section><h2 className="font-black">{t.tasks}</h2>{snapshot.tasks.map(item=><article key={item.id} className="mt-2 border bg-white p-3"><strong>{item.title}</strong><p className="text-xs">{item.status}</p></article>)}</section><section><h2 className="flex items-center gap-2 font-black"><MessageSquareText className="size-4" aria-hidden="true"/>{t.comments}</h2>{snapshot.comments.map(item=><p key={item.id} className="mt-2 border bg-white p-3">{item.body}</p>)}</section><section><h2 className="font-black">{t.files}</h2>{snapshot.files.map(item=><article key={item.id} className="mt-2 border bg-white p-3"><strong>{item.displayName}</strong><button aria-label={`${t.download}: ${item.displayName}`} onClick={()=>void client.requestDownload(organizationId,item.id)} className="mt-2 grid size-9 place-items-center rounded-md border"><Download className="size-4" aria-hidden="true"/></button></article>)}</section></div><button className="mt-6 inline-flex items-center gap-2 rounded-md bg-teal-800 px-4 py-2 font-bold text-white"><Plus className="size-4" aria-hidden="true"/>{t.request}</button></div></main>}
export function ClientPortalPage(){const{organizationSlug='',projectId}=useParams();const{organizationId}=useTenant();if(!organizationId)return<main dir="rtl">{t.noTenant}</main>;return projectId?<PortalProjectScreen organizationId={organizationId} projectId={projectId} client={portalClient}/>:<PortalDashboardScreen organizationId={organizationId} slug={organizationSlug} client={portalClient}/>}
