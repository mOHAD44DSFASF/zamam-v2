import { CalendarDays, Check, LoaderCircle, Send } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import { useTenant } from '../../tenant/tenant-context'
import { attendanceLeaveClient, type AttendanceLeaveClient, type AttendanceLeaveSnapshot } from './client'
const t = {
  title: '\u0627\u0644\u062d\u0636\u0648\u0631 \u0648\u0627\u0644\u0625\u062c\u0627\u0632\u0627\u062a', loading: '\u062c\u0627\u0631\u064d \u0627\u0644\u062a\u062d\u0645\u064a\u0644...', error: '\u062a\u0639\u0630\u0631 \u062a\u062d\u0645\u064a\u0644 \u0628\u064a\u0627\u0646\u0627\u062a \u0627\u0644\u062d\u0636\u0648\u0631', noTenant: '\u0644\u0627 \u062a\u0648\u062c\u062f \u0639\u0636\u0648\u064a\u0629 \u0646\u0634\u0637\u0629.', today: '\u062d\u0636\u0648\u0631 \u0627\u0644\u064a\u0648\u0645', leave: '\u0637\u0644\u0628 \u0625\u062c\u0627\u0632\u0629', type: '\u0646\u0648\u0639 \u0627\u0644\u0625\u062c\u0627\u0632\u0629', start: '\u0645\u0646', end: '\u0625\u0644\u0649', reason: '\u0627\u0644\u0633\u0628\u0628', send: '\u0625\u0631\u0633\u0627\u0644', requests: '\u0637\u0644\u0628\u0627\u062a\u064a', approvals: '\u0637\u0644\u0628\u0627\u062a \u062a\u0646\u062a\u0638\u0631 \u0627\u0644\u0627\u0639\u062a\u0645\u0627\u062f', approve: '\u0627\u0639\u062a\u0645\u0627\u062f', external: '\u0627\u0644\u0631\u0635\u064a\u062f \u0645\u062f\u0627\u0631 \u0645\u0646 \u0646\u0638\u0627\u0645 HR \u062e\u0627\u0631\u062c\u064a',
}
export function AttendanceLeaveScreen({ organizationId, client }: { organizationId: string; client: AttendanceLeaveClient }) {
  const [snapshot, setSnapshot] = useState<AttendanceLeaveSnapshot | null>(null)
  const [status, setStatus] = useState<'loading'|'ready'|'error'>('loading')
  const [typeId, setTypeId] = useState(''); const [startsOn, setStartsOn] = useState(''); const [endsOn, setEndsOn] = useState(''); const [reason, setReason] = useState('')
  const load = useCallback(async () => { setStatus('loading'); try { const value = await client.load(organizationId); setSnapshot(value); setTypeId(value.leaveTypes[0]?.id ?? ''); setStatus('ready') } catch { setStatus('error') } }, [client, organizationId])
  useEffect(() => { let active = true; client.load(organizationId).then((value) => { if (active) { setSnapshot(value); setTypeId(value.leaveTypes[0]?.id ?? ''); setStatus('ready') } }, () => { if (active) setStatus('error') }); return () => { active = false } }, [client, organizationId])
  if (status === 'loading') return <main dir="rtl" className="grid min-h-screen place-items-center"><p role="status"><LoaderCircle className="inline size-5 animate-spin" aria-hidden="true" /> {t.loading}</p></main>
  if (status === 'error' || !snapshot) return <main dir="rtl" className="grid min-h-screen place-items-center"><h1>{t.error}</h1></main>
  return <main dir="rtl" className="min-h-screen bg-gray-50"><header className="border-b bg-white"><div className="mx-auto max-w-6xl px-5 py-6"><h1 className="text-2xl font-black">{t.title}</h1></div></header><div className="mx-auto grid max-w-6xl gap-6 px-5 py-6 lg:grid-cols-[1fr_340px]"><div className="space-y-6"><section className="border bg-white p-5" aria-labelledby="today-heading"><h2 id="today-heading" className="flex items-center gap-2 font-black"><CalendarDays className="size-5 text-teal-800" aria-hidden="true" />{t.today}</h2><p className="mt-3 text-sm">{snapshot.today ? `${snapshot.today.date} · ${snapshot.today.status} · ${(snapshot.today.workedMinutes/60).toFixed(1)} h` : '\u0644\u0645 \u064a\u0633\u062c\u0644 \u0627\u0644\u062d\u0636\u0648\u0631 \u0628\u0639\u062f'}</p></section><section aria-labelledby="requests-heading"><h2 id="requests-heading" className="mb-3 font-black">{t.requests}</h2>{snapshot.myRequests.map((request) => <article key={request.id} className="mb-3 border bg-white p-4"><h3 className="font-bold">{request.typeName}</h3><p className="text-sm text-gray-600">{request.startsOn} - {request.endsOn} · {request.quantityDays} · {request.status}</p></article>)}</section>{snapshot.capabilities.approveLeave && <section aria-labelledby="approvals-heading"><h2 id="approvals-heading" className="mb-3 font-black">{t.approvals}</h2>{snapshot.approvalQueue.map((request) => <article key={request.id} className="mb-3 flex flex-wrap items-center justify-between gap-3 border bg-white p-4"><div><h3 className="font-bold">{request.employeeName}</h3><p className="text-sm text-gray-600">{request.typeName} · {request.startsOn} - {request.endsOn}</p></div><button onClick={() => void client.decideLeave(organizationId, request.id, request.version, 'approved').then(load)} className="inline-flex items-center gap-2 rounded-md bg-teal-800 px-3 py-2 font-bold text-white"><Check className="size-4" aria-hidden="true" />{t.approve}</button></article>)}</section>}</div>{snapshot.capabilities.requestLeave && <aside><form onSubmit={(event) => { event.preventDefault(); void client.requestLeave(organizationId, { id: crypto.randomUUID(), leaveTypeId: typeId, startsOn, endsOn, reason }).then(load) }} className="border bg-white p-5"><h2 className="font-black">{t.leave}</h2><label className="mt-4 block text-sm font-bold">{t.type}<select required value={typeId} onChange={(event) => setTypeId(event.target.value)} className="mt-1 w-full rounded-md border p-2">{snapshot.leaveTypes.map((type) => <option key={type.id} value={type.id}>{type.name}{type.remainingDays === null ? ` · ${t.external}` : ` · ${type.remainingDays}`}</option>)}</select></label><label className="mt-3 block text-sm font-bold">{t.start}<input required type="date" value={startsOn} onChange={(event) => setStartsOn(event.target.value)} className="mt-1 w-full rounded-md border p-2" /></label><label className="mt-3 block text-sm font-bold">{t.end}<input required type="date" value={endsOn} onChange={(event) => setEndsOn(event.target.value)} className="mt-1 w-full rounded-md border p-2" /></label><label className="mt-3 block text-sm font-bold">{t.reason}<textarea required minLength={3} maxLength={500} value={reason} onChange={(event) => setReason(event.target.value)} className="mt-1 min-h-20 w-full rounded-md border p-2" /></label><button disabled={!typeId || !startsOn || !endsOn || reason.trim().length < 3} className="mt-4 inline-flex items-center gap-2 rounded-md bg-teal-800 px-4 py-2 font-bold text-white disabled:opacity-50"><Send className="size-4" aria-hidden="true" />{t.send}</button></form></aside>}</div></main>
}
export function AttendanceLeavePage() { const { organizationId } = useTenant(); if (!organizationId) return <main dir="rtl">{t.noTenant}</main>; return <AttendanceLeaveScreen organizationId={organizationId} client={attendanceLeaveClient} /> }
